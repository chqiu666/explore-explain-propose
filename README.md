# explore-explain-propose

🚀 **探索、解释、提案** - 一个创新的项目

## 项目简介

这是一个使用 GitHub Pages 部署的网页项目，展示了"探索、解释、提案"的三个核心步骤。

## 在线访问

网站已部署在 GitHub Pages 上，访问地址：
- https://chqiu666.github.io/explore-explain-propose/

## 本地预览

要在本地预览网页，只需在浏览器中打开 `index.html` 文件即可。

或者使用简单的 HTTP 服务器：

```bash
# 使用 Python
python -m http.server 8000

# 使用 Node.js (需要安装 http-server)
npx http-server
```

然后在浏览器中访问 `http://localhost:8000`

## GitHub Pages 设置

本项目使用 GitHub Actions 自动部署到 GitHub Pages。每次推送到 `main` 分支时，都会自动触发部署流程。

### 配置步骤

1. 在仓库的 Settings > Pages 中
2. Source 选择 "GitHub Actions"
3. 推送代码后会自动部署

## 项目结构

```
.
├── index.html              # 主页面
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Actions 部署配置
├── .gitignore             # Git 忽略文件
└── README.md              # 项目说明
```

## 许可证

MIT